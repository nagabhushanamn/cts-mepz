

var person = {
    name: "Nag",
    sub:'.js'
};

// var message = "The person " + person.name + " teaching " + person.sub;
// var message = `The person ${person.name} teaching ${person.sub}`;


var htmlTemplate = `<div>
                        <ul>
                            <li>${person.name}</li>
                        </ul>
                    </div>`;


